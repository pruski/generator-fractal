angular.module('<%= camelCasedName %>').directive('<%= camelCasedName %>', [

function(){
    'use strict';

    return {
        restrict    : 'E',
        replace     : true,<% if (ctrl) { %>
        controller  : '<%= camelCasedName %>Ctrl',<% } %><% if (tpl) { %>
        templateUrl : '<%= templateCacheName %>',<% } else { %>
        template    : '<div class="<%= dashedName %>"></div>'<% } %><% if (link) { %>
        link: link,<% } %>
        scope: {}
    };
<% if (link) { %>
    function link(scope, element) {

    }
<% } %>
}]);